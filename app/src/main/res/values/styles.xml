<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorAccent</item>
        <item name="colorAccent">@color/colorPrimaryDark</item>
        <item name="android:statusBarColor">@color/colorAccent_2</item>
    </style>

    <style name="themeOfTabbingActivity" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:textColor">@color/BColorPrimary</item>
        <item name="android:subtitle">Hello</item>
        <item name="android:titleTextColor" tools:ignore="NewApi">@color/BColorPrimary</item>
        <item name="colorAccent">@color/colorAccent_2</item>
        <item name="colorPrimaryDark">@android:color/white</item>
    </style>

    <style name="dialogTimePicker" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:background">@android:color/transparent</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

</resources>
